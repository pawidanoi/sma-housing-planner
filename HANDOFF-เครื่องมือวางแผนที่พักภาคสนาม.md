# HANDOFF — เครื่องมือวางแผนที่พักภาคสนาม (สำหรับ AREA)

> เอกสารนี้สรุปทุกอย่างจากขั้นตอนออกแบบ+ทำ mockup (คุยกันในแชทหลายรอบ) เพื่อส่งต่อให้ทีม/AI
> session ที่จะไปเขียนเป็นระบบจริงในโค้ด อ่านไฟล์นี้ไฟล์เดียวควรเข้าใจได้ว่า **ระบบนี้ต้องทำอะไร
> ทำไมถึงออกแบบแบบนี้ กฎธุรกิจคืออะไร ข้อมูลจริงมีอะไรอยู่แล้ว และมีช่องว่างอะไรที่ต้องปิดก่อนใช้จริง**
>
> **สถานะตอนนี้คือ mockup/prototype เท่านั้น** — ไฟล์ HTML เดี่ยว รันในเบราว์เซอร์ล้วน
> (ไม่มี backend, ไม่มีฐานข้อมูล, ข้อมูลอยู่ใน memory เท่านั้น รีเฟรชหน้าเว็บ = ข้อมูลหาย) ใช้ demo
> แนวคิด/คำนวณ/UX ให้ผู้ใช้ (ผู้บริหาร) ดูและอนุมัติแนวทางก่อน ยังไม่ใช่ระบบที่ใช้งานจริงได้

---

## 1. ระบบนี้คืออะไร (สรุป 3 บรรทัด)

ทีมภาคสนาม SMA ต้องเดินทางไปทำงานที่สาขาต่างๆ ทั่วประเทศและต้องพักค้างคืน — เดิมแอดมินคนเดียวจอง
ที่พักให้ทุกทีม แต่ทีมขยายเร็วจนแอดมินจองไม่ไหว จึง **ย้ายให้ AREA แต่ละคน (ดูแลแค่ไม่กี่ทีม SMA)
เป็นคนจองเองให้ทีมตัวเอง** ระบบนี้คือ **เครื่องมือคำนวณ/แดชบอร์ดช่วยตัดสินใจล้วนๆ** (ไม่มีปุ่มจอง
ไม่มีสถานะอนุมัติในระบบ) ช่วย AREA เลือกที่พักที่ใกล้สาขาที่สุด รวมที่พักกับทีมอื่นได้เมื่อคุ้ม
และช่วยผู้บริหารตรวจสอบได้ก่อน/หลังโดยดูจากแดชบอร์ดเดียวกัน

**การจองจริงยังเกิดขึ้นนอกระบบนี้เสมอ** (ผ่านระบบจองโรงแรม Choowap ที่มีอยู่แล้ว) — เครื่องมือนี้
แค่บอกว่า "ควรจองที่ไหน" ไม่ได้ทำการจองเอง

---

## 2. ทำไมถึงออกแบบแบบนี้ (ประวัติการตัดสินใจ — สำคัญ อย่าย้อนกลับโดยไม่รู้ที่มา)

ระหว่างคุยออกแบบ มีการเปลี่ยนทิศทางหลายรอบ สรุปเป็นเส้นเวลาเพื่อไม่ให้ทีมใหม่งงว่าทำไมไม่มีฟีเจอร์
ที่ "น่าจะต้องมี" บางอย่าง:

1. เริ่มจากคิดเป็นระบบเต็มรูป (AREA จอง → ส่งขออนุมัติ → ผู้บริหารอนุมัติ/ตีกลับ มีสถานะ
   draft/pending/approved ในระบบ)
2. ผู้ใช้ตัดสินใจ **ตัดชั้นอนุมัติในระบบทิ้งทั้งหมด** — เหตุผลคือไม่ต้องการให้ AREA ต้อง "รายงาน"
   อะไรเป็นทางการ อยากได้แค่เครื่องมือช่วยจอง + เครื่องมือช่วยตรวจสอบ ให้ผู้บริหารดูเทียบเอง
   นอกระบบ (คุยกัน/โทรถาม) ไม่ใช่กดอนุมัติในระบบ
3. เคยมีแท็บ "ตรวจสอบย้อนหลัง" เทียบกับข้อมูลจองจริงจาก Choowap (แนะนำ vs จองจริง) — **ถูกเอาออก
   ทั้งหมดในภายหลัง** เพราะผู้ใช้ยืนยันว่า "จะทำเป็นแค่เครื่องมือช่วยส่วนข้อมูลพิกัด" ไม่ต้องมีส่วน
   เทียบกับข้อมูลจองจริง **ถ้าจะเพิ่มฟีเจอร์นี้กลับมาต้องถามผู้ใช้ก่อน อย่าเพิ่มเองเพราะเคยมี**
4. เรื่อง "พนักงานใหม่ต้องจองแยกใบ รอยืนยันมาก่อนอนุมัติ" ที่คุยกันตอนทำ SOP นำเสนอ — **เป็นกฎของ
   กระบวนการที่มนุษย์ทำเอง (ผู้บริหาร reject ด้วยมือ) ไม่ใช่ฟีเจอร์ในเครื่องมือนี้** อย่าไปสร้าง
   สถานะ/workflow ในระบบเพื่อรองรับกฎข้อนี้

**สรุปหลักการที่ต้องคงไว้เสมอ:** ระบบนี้คือ **calculation/decision-support only** — มีแต่ "มุมมอง"
(view) ที่คำนวณจากข้อมูลจริง ไม่มี "การกระทำ" (action) ที่เปลี่ยนสถานะการจองใดๆ ทั้งสิ้น

---

## 3. ผู้ใช้งานและสิทธิ์

- ผู้ใช้หลัก: **AREA ทุกคน** — เห็นข้อมูลเหมือนกันหมด ไม่มีการจำกัดสิทธิ์ตามทีม/พื้นที่ที่ดูแล
  (ตัดสินใจแล้วว่าไม่ต้องทำ auth scoping ซับซ้อน — ทุกคนเห็นแผนงาน/สาขา/ที่พักทั้งหมด)
- ผู้บริหาร: ใช้แดชบอร์ดเดียวกันกับ AREA เพื่อตรวจสอบเทียบ ไม่มีหน้าจอแยก ไม่มีสิทธิ์พิเศษในระบบ
- ไม่มีระบบ login/authentication ใน mockup นี้ — ถ้าทำระบบจริงต้องตัดสินใจเองว่าจะเพิ่มหรือไม่
  (ผู้ใช้ไม่เคยขอ auth มาก่อน)

---

## 4. โครงสร้างข้อมูล (Data Model)

4 entity หลัก ไม่มี booking/approval entity ใดๆ:

### `branches` (สาขา)
| field | ชนิด | หมายเหตุ |
|---|---|---|
| `c` (code) | string | รหัสสาขา รูปแบบ `CJX#####` — **คีย์หลักที่ทุกตารางอ้างอิง** |
| `n` (name) | string | ชื่อสาขาภาษาไทย |
| `d` (district) | string | อำเภอ |
| `pv` (province) | string | จังหวัด |
| `la`, `lo` | float | พิกัด (ต้องมีเสมอ ถ้าไม่มีพิกัดใช้คำนวณระยะทางไม่ได้เลย) |
| `status` | string | สถานะสาขา (Active store, Set up ร้าน, Cancel, Prospect ฯลฯ) — **เก็บไว้เป็นข้อมูลเสริม
  ไม่ได้ใช้กรองอะไรในการคำนวณปัจจุบัน** เพราะทีม SMA ทำงานที่สาขา "ก่อนเปิด" ด้วย (งานแฟร์เปิดใหม่)
  ห้ามกรองเฉพาะ Active store ออกมาเฉยๆ โดยไม่ถาม |

### `hotels` (ที่พัก)
| field | ชนิด | หมายเหตุ |
|---|---|---|
| `n` (name) | string | ชื่อที่พัก |
| `pr` (price) | number | ราคา/คืน (บาท) |
| `d`, `pv` | string | อำเภอ, จังหวัด |
| `la`, `lo` | float | พิกัด |

### `employees` (พนักงาน)
| field | ชนิด | หมายเหตุ |
|---|---|---|
| `name` | string | ชื่อเต็ม |
| `team` | string | รหัสทีม (SMA1-16, Admin, Area, HQ) — **ดูหัวข้อ 6 เรื่องคอลัมน์นี้มาจากไหนในไฟล์จริง** |
| `code`, `nickname`, `gender`, `area_incharge`, `phone` | string | ข้อมูลเสริม ไม่ใช้ในการคำนวณ
  แต่เก็บไว้เผื่อใช้ต่อ |
| `home_la`, `home_lo` | float \| null | พิกัดบ้าน — **null ได้ (ปัจจุบันมีจริงแค่ ~54% ของพนักงาน
  ทั้งหมด ดูหัวข้อ 8)** จุดไหน null ต้องข้ามการคำนวณ "บ้านใกล้สาขา" ให้คนนั้น ไม่ใช่ error |

### `schedule_items` (แผนงาน) — ตารางที่ **มีการกระทำได้** (เพิ่ม/แก้/ลบ) ตารางเดียวในระบบ
| field | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | string | unique id |
| `team` | string | ทีมที่ไปทำงาน |
| `branches` | string[] | รหัสสาขา — **เป็น array เพราะบางงาน 1 ทีมไป 2 สาขาพร้อมกัน** |
| `work_start`, `work_end` | date (ISO) | วันจัดงานจริง — **ใช้ตัดสินใจว่า "งานวันเดียวกันไหม"
  สำหรับกฎรวมที่พัก/หาจุดกลาง (ดูหัวข้อ 5)** |
| `stay_start`, `stay_end` | date (ISO) | วันเข้าพัก-ออกจากที่พัก (มักเร็ว/ช้ากว่าวันทำงานจริง 1 วัน)
  — **ใช้คำนวณจำนวนคืนสำหรับประมาณราคาเท่านั้น ไม่ใช้ตัดสินการรวมที่พัก** |
| `male`, `female` | number \| null | จำนวนคนแยกเพศ — **null ได้เสมอ** (ไฟล์แผนงานจริงจาก
  ผู้ใช้ไม่มีคอลัมน์นี้) ต้องมี UI ให้กรอกเพิ่มทีหลังได้ ห้ามคำนวณราคา/ห้องถ้าเป็น null (ต้องโชว์
  "ยังไม่ระบุจำนวนคน" แทนเลขมั่ว) |
| `job_type` | string \| null | ประเภทงาน (เช่น "งานแฟร์เปิดใหม่ 200K") — ข้อมูลเสริม แสดงเป็น tag |
| `needs_burmese` | boolean | ต้องการล่ามพม่าไหม — ข้อมูลเสริม แสดงเป็น tag |

**แยก `work_start/work_end` กับ `stay_start/stay_end` เป็นคนละฟิลด์เสมอ** — เป็นบทเรียนตรงจาก
ระบบเดิม (`sma-accommodation-app`) ที่เคยใช้ปนกันจนสับสน และตอนนี้ยิ่งสำคัญเพราะกฎรวมที่พัก/หาจุด
กลางอ้างอิง work date ไม่ใช่ stay date (ดูหัวข้อ 5)

---

## 5. กฎธุรกิจ / สูตรคำนวณ ทั้งหมด (ยืนยันแล้วกับผู้ใช้)

| กฎ | ค่า | ใช้ที่ไหน |
|---|---|---|
| ที่พักต้องห่างจากสาขาไม่เกิน | **15 กม.** | ทุกเครื่องมือแนะนำที่พัก (nearest, cluster shared-hotel, midpoint candidate filter) |
| สูตรคิดจำนวนห้อง | `ceil(ชาย/2) + ceil(หญิง/2)` | ประมาณราคา (ต้องมี male/female ไม่เป็น null) |
| เกณฑ์ "สาขาใกล้กันพอจะรวมที่พักได้" | **≤30 กม.** ระหว่างสาขา (ปรับได้ 20/30/40 ในมockup) | รวมที่พักได้ + จุดกลาง 2 สาขา (auto-detect) |
| เงื่อนไข "รวมกันได้" ต้องจัดงาน**วันเดียวกัน** | เช็คจาก `work_start`/`work_end` **ทับกัน** (ไม่ใช่ stay date) | รวมที่พักได้ + จุดกลาง 2 สาขา |
| รวมที่พักได้ = สองทีมต่างกัน คนละสาขา ใกล้กัน วันตรงกัน | — | คำนวณที่พักร่วมที่ดีสุด: `min(dist(hotel,A) + dist(hotel,B))` โดยต้อง ≤15กม.จากทั้งคู่ |
| จุดกลาง 2 สาขา = **ทีมเดียวกัน** ทำงาน 2 สาขาพร้อมกัน (พบไม่บ่อย) | — | คำนวณคะแนน `score = dist(hotel,A)×wA + dist(hotel,B)×wB` (ถ่วงน้ำหนักตามจำนวนคนได้) เลือกคะแนนต่ำสุด |
| พนักงานบ้านใกล้สาขา ไม่ต้องจองที่พัก | **≤10 กม.** (เข้มงวด) หรือ **≤30 กม.** (หลวม) สลับดูได้ | เครื่องมือ "บ้านพนักงานใกล้สาขา" |
| งานถือว่า "จบแล้ว" ย้ายไปหน้าจัดเก็บอัตโนมัติ | `work_end < วันนี้` | ทุกการคำนวณ ไม่รวมงานที่จัดเก็บแล้ว |
| Scope "แสดงเฉพาะสาขาที่มีแผนงาน" | toggle เปิด/ปิดได้ ค่าเริ่มต้นเปิด | ถ้าไม่มีแผนงานเลย fallback = โชว์ทั้งทะเบียน (ไม่ใช่ว่างเปล่า) — ดูหัวข้อ 7 เรื่อง scope vs เลือกเอง |

**กฎที่จงใจไม่ทำ (เคยคุยแล้วตัดออก):** ไม่มีเพดานงบประมาณที่ block การอนุมัติ, ไม่มีการเช็คสถานะ
สาขา (Active/Cancel/Prospect) เป็นเงื่อนไขกรอง, ไม่มีการล็อกจำนวนผู้ใช้พร้อมกัน

---

## 6. เครื่องมือทั้งหมดในระบบ (6 มุมมอง)

1. **🗺️ ภาพรวม** — แผนที่ (จุดสี: แดง=ไม่มีที่พักในระยะ15กม., เหลือง=มีน้อย≤2, เขียว=พอ) + ตาราง
   ทุกสาขา (เรียงตามความเร่งด่วน) กดดูรายละเอียดที่พักในระยะได้ต่อแถว + ช่องอัพโหลด/เพิ่มแผนงาน +
   ช่องอัพโหลดทะเบียนอ้างอิง (สาขา/ที่พัก/พนักงาน) + งานที่ "1 ทีม 2 สาขา" ที่ตรวจพบอัตโนมัติ
2. **🏨 ที่พักแนะนำตามสาขา** — โหมด "ทุกสาขาตามแผนงาน" (การ์ดต่อกันทุกสาขาที่ scope ไว้) หรือ
   "เลือกสาขาเอง" (dropdown เต็มทะเบียนเสมอ ไม่ผูก scope) เรียงที่พักใกล้สุดพร้อมราคา
3. **🤝 รวมที่พักได้** — ดูหัวข้อ 5
4. **🚗 จุดกลาง 2 สาขา** — เลือก 2 สาขาเอง (dropdown เต็มทะเบียนเสมอ) คำนวณที่พักที่เหมาะสุด
   + auto-detect งาน "1 ทีมเดียว 2 สาขา" จากแผนงานโชว์ในแท็บภาพรวม กดกระโดดมาที่นี่ได้เลย
5. **🏠 บ้านพนักงานใกล้สาขา** — โหมด "ทุกงานตามแผนงาน" (จับคู่ทีมในแผนงานกับพนักงานทีมนั้น) หรือ
   "เลือกสาขาเอง" (เทียบพนักงานทุกคนกับสาขาที่เลือก)
6. **🗄️ จัดเก็บ** — งานที่ `work_end` ผ่านไปแล้ว ย้ายมาที่นี่อัตโนมัติ ไม่รวมในการคำนวณหน้าอื่น
   มีปุ่มลบถาวร

**สำคัญ: มี 2 concept ที่ต้องแยกให้ชัดเวลาสร้างจริง**
- **Scope (แสดงเฉพาะสาขาที่มีแผนงาน)** — มีผลเฉพาะกับ "มุมมองอัตโนมัติ" (ตารางภาพรวม, แผนที่,
  โหมด "ทุกสาขาตามแผนงาน") ถ้าไม่มีแผนงานเลย fallback โชว์ทั้งทะเบียน ไม่ใช่ว่างเปล่า
- **เลือกสาขาเอง (dropdown ทุกเครื่องมือ)** — ต้องใช้ทะเบียนเต็มเสมอ ห้ามผูกกับ scope/แผนงาน
  ไม่งั้นพอไม่มีแผนงาน dropdown จะว่าง เลือกอะไรไม่ได้เลย (บั๊กที่เจอจริงระหว่างทำ mockup)

---

## 7. บทเรียนจากการอ่านไฟล์จริงของผู้ใช้ (สำคัญมากสำหรับ import logic)

ทดสอบกับไฟล์จริง 3 ไฟล์ระหว่างทำ mockup เจอปัญหาที่ควรรู้ล่วงหน้า:

### ไฟล์แผนงาน (`plan_sep.xlsx`)
คอลัมน์จริง: `รหัสสาขา, สาขา, จังหวัด, ชาวพม่า, ประเภทงาน, Status, จำนวนวันที่, วันที่เริ่มงาน, วัน,
วันที่จบงาน, วัน (ซ้ำ), ทีมที่ไป, วันที่เข้างาน`
- **ไม่มีคอลัมน์จำนวนคน (ชาย/หญิง) เลย** — ต้องออกแบบ UI ให้กรอกเพิ่มทีหลังเสมอ
- คอลัมน์ `Status` มีค่าเดียวคือ `Confirm` ในไฟล์ตัวอย่าง — ควร import เฉพาะแถวที่ status เป็น
  Confirm (หรือ null) ข้ามแถวอื่น
- วันที่เป็น Excel date serial/Date object ต้องแปลงเป็น ISO string
- **พบจริง: 1 ทีมไป 2 สาขาพร้อมกัน (วันทำงานทับกัน) เกิดขึ้นจริงในข้อมูล** — ต้อง auto-detect ไม่ใช่
  ให้ผู้ใช้มานั่ง merge เอง

### ไฟล์ทะเบียนสาขา (`สาขาใหม่.xlsx`)
คอลัมน์จริง: `รหัสสาขา, CJX Store Code, ชื่อสาขา, Store Name, ตำบล, อำเภอ, จังหวัด, ที่อยู่เต็ม,
Google Maps, ละติจูด, ลองจิจูด, คลังสินค้า, สัดส่วนชาวพุทธ/มุสลิม/พม่า/กัมพูชา, Zoning, Grand
opening, Status`

**⚠️ กับดักสำคัญที่สุด:** คอลัมน์ชื่อ `รหัสสาขา` ในไฟล์นี้เป็น**รหัสภายใน** (เช่น `T004`) **ไม่ใช่**
รหัส `CJX#####` ที่ไฟล์แผนงานใช้อ้างอิง — รหัสที่ถูกต้องอยู่ในคอลัมน์ `CJX Store Code` ต่างหาก! ถ้า
parser ไปอ่าน `รหัสสาขา` ตรงๆ จะจับคู่กับไฟล์แผนงานไม่ได้เลยสักแถว (เจอบั๊กนี้จริงระหว่างทดสอบ)
**กติกา import: ถ้ามีคอลัมน์ชื่อคล้าย "CJX Store Code" ให้ใช้อันนั้นก่อนเสมอ อย่าเชื่อชื่อคอลัมน์
"รหัสสาขา" เพียงอย่างเดียว**

พิกัดเป็นคอลัมน์ ละติจูด/ลองจิจูด แยกกันตรงๆ (ไม่มีปัญหา) มี ~234 แถว มีบางแถว (4 แถว) ไม่มีพิกัด/
ชื่อ ต้องข้าม

### ไฟล์รายชื่อพนักงาน (`รายชื่อใหม่เต็มข้อมูล.xlsx`)
คอลัมน์จริง: `รหัสพนักงาน, ชื่อพนักงาน, ตำแหน่ง, Areaดูแล, ชื่อเล่น, เพศ, ที่อยู่, พิกัด, เบอร์โทร`

- **คอลัมน์ `พิกัด` เป็นค่าเดียวรวมกัน** รูปแบบ `"ละติจูด, ลองจิจูด"` ในเซลล์เดียว (เช่น
  `"13.76, 99.74"`) ต้อง split ด้วย comma เอง ไม่ใช่ 2 คอลัมน์แยก
- **คอลัมน์ `ตำแหน่ง` ใช้แทนทั้งสองความหมายปนกัน**: บางแถวเป็นรหัสทีม (`SMA5`) บางแถวเป็นตำแหน่งงาน
  จริง (`Admin`, `Area`, `HQ`) — import แบบไม่กรอง เก็บตามที่มีจริง อย่าพยายามเดา/แยกเอง
- ค่าว่าง/ไม่มีข้อมูลบางเซลล์เป็น string `"nan"` ตรงๆ (ไม่ใช่ null จริง) ต้องเช็ค
  `/^nan$/i` ด้วย ไม่ใช่แค่ `== null`

---

## 8. ข้อมูลจริงที่มีอยู่แล้ว (แนบมากับ handoff นี้ — ใช้เป็น seed data ได้เลย)

| ไฟล์ | จำนวน | หมายเหตุ |
|---|---|---|
| `branches.csv` | 230 แห่ง | จากไฟล์ทะเบียนสาขาล่าสุดที่ผู้ใช้ให้มา (ใช้ `CJX Store Code` เป็นรหัสแล้ว) |
| `hotels.csv` | 207 แห่ง | จากทะเบียนที่พักระบบเดิม (ตรวจพิกัดครบ 100% แล้วตั้งแต่โปรเจกต์ก่อน) |
| `employees.csv` | 114 คน | จากไฟล์รายชื่อล่าสุด — **มีพิกัดบ้านแค่ 61 คน (54%) อีก 53 คนยังไม่มี
  พิกัด** ต้องขอเพิ่มจากพนักงานจริงถ้าอยากให้เครื่องมือ "บ้านใกล้สาขา" ครอบคลุมทุกคน |

**หมายเหตุสำคัญ:** ทะเบียนสาขา 230 แห่งนี้อาจ**ไม่ครบ 100%** ของสาขาจริงทั้งหมด (โปรเจกต์ระบบเดิม
เคยมีบันทึกไว้ว่ามี 245 สาขา) — ตอน import แผนงานจริงอาจเจอรหัสสาขาที่ไม่พบในทะเบียนนี้ ระบบต้อง
handle กรณีนี้เสมอ (ข้ามแถว + แจ้งเตือนชัดเจนว่ารหัสอะไรหาไม่เจอ ไม่ใช่ fail เงียบๆ)

---

## 9. สถาปัตยกรรมที่แนะนำสำหรับระบบจริง (mockup ไม่มีส่วนนี้เลย)

mockup ปัจจุบันเป็น **ไฟล์ HTML เดี่ยว, ข้อมูลอยู่ใน JS variable ใน memory, ไม่มี backend** — ใช้
SheetJS (xlsx) อ่านไฟล์ที่ผู้ใช้อัพโหลดฝั่ง browser ล้วนๆ ระบบจริงต้องมีอย่างน้อย:

- **ฐานข้อมูล**: ตาราง `branches`, `hotels`, `employees`, `schedule_items` ตามโครงสร้างหัวข้อ 4
  (โครงสร้างเรียบง่าย ไม่มี booking/approval table เพราะระบบนี้ไม่มี concept นั้น)
- **Backend API** สำหรับ: CRUD ทั้ง 4 ตาราง + endpoint upload ไฟล์ (Excel/CSV) พร้อม column-mapping
  ตามหัวข้อ 7 (แนะนำใช้ library เดียวกับแนวคิด SheetJS ฝั่ง Node หรือ pandas/openpyxl ฝั่ง Python)
- **Multi-user**: ตอนนี้ mockup เป็น single-user (ข้อมูลในเบราว์เซอร์ตัวเอง ไม่ sync กับใคร) —
  ระบบจริงต้องมี DB กลางให้ AREA ทุกคนเห็นข้อมูลเดียวกันแบบ real-time หรืออย่างน้อย refresh ได้
- **เลือก hosting ที่ไม่จำกัดจำนวน serverless functions ตั้งแต่ต้น** — โปรเจกต์ระบบจองที่พักเดิม
  (`sma-accommodation-app`) เจอปัญหาติด Vercel Hobby cap 12 functions มาตลอด ทำให้โค้ดสกปรกจาก
  การยัดทุกอย่างเข้าไฟล์เดียว ระบบใหม่ควรเลี่ยงปัญหานี้ตั้งแต่ต้น
- คำนวณระยะทาง (haversine) ทำฝั่งไหนก็ได้ (client หรือ server) — สูตรง่าย ไม่จำเป็นต้องพึ่ง Google
  Distance Matrix API เว้นแต่อยากได้ระยะทางถนนจริงแทนเส้นตรง (เคยพูดถึงไว้เป็นตัวเลือกอนาคต ไม่ใช่
  requirement ตอนนี้)

## 10. ช่องว่างที่ต้องปิดก่อนใช้งานจริง (เรียงตามความสำคัญ)

1. **ไม่มี persistence เลย** — ปิด/รีเฟรชหน้าเว็บ = ข้อมูลที่อัพโหลด/เพิ่มหายหมด (ทั้งแผนงานและ
   ทะเบียนที่แก้ไข) ต้องมี backend+DB ก่อนใช้จริงแน่นอน เป็นช่องว่างอันดับ 1
2. **ทะเบียนสาขาอาจไม่ครบ 100%** (230 vs 245 ที่เคยบันทึกไว้) — ควรหาไฟล์ทะเบียนสาขาฉบับสมบูรณ์
   ที่สุดมาแทนก่อน deploy จริง
3. **พนักงาน 53 คนยังไม่มีพิกัดบ้าน (46%)** — ต้องมีกระบวนการเก็บพิกัดเพิ่ม (เช่น ฟอร์มให้กรอกเอง/
   ปักหมุดเอง) ไม่งั้นเครื่องมือ "บ้านใกล้สาขา" ใช้ได้ไม่เต็มประสิทธิภาพ
4. **ไม่มี multi-user sync** — ตอนนี้ต่างคนต่างเห็นข้อมูลของตัวเอง (ถ้าเปิดคนละเครื่อง) ต้องแก้เมื่อ
   มี backend แล้ว
5. **อ่านได้แค่ sheet แรกของไฟล์ Excel เสมอ** — ถ้าไฟล์จริงมีหลาย sheet ที่ต้องใช้ ต้องเพิ่ม UI ให้
   เลือก sheet เอง (mockup ปัจจุบัน hardcode `wb.SheetNames[0]`)
6. **ไม่มี validation ละเอียดระดับ field** (เช่น เบอร์โทรผิดรูปแบบ, ชื่อซ้ำ) — import ตอนนี้เช็คแค่
   ฟิลด์จำเป็นครบไหม/พิกัดเป็นตัวเลขไหม

---

## 11. บทเรียนจากระบบจองที่พักเดิม (`sma-accommodation-app`) ที่ยังใช้ได้กับระบบนี้

ระบบเดิมเป็นระบบที่แอดมินจองให้พนักงาน (ปิดโปรเจกต์ไปแล้ว) มีบทเรียนที่ยังเอามาใช้ต่อได้:

- **Generated columns บังคับ** (ห้อง/ยอดรวมคำนวณจากสูตรเสมอ ห้ามพิมพ์เอง) — ใช้หลักการเดียวกันกับ
  สูตรคิดห้องในหัวข้อ 5 อยู่แล้ว
- **แยกวันทำงานจริงกับวันเข้าพักเป็นคนละฟิลด์ตั้งแต่ schema แรก** — ทำไว้แล้วในระบบนี้ (หัวข้อ 4)
- **อย่าเชื่อพิกัดจากไฟล์ import โดยไม่ตรวจสอบ** — ระบบเดิมเจอปัญหาพิกัดที่พักผิดกระจายทั่วทะเบียน
  660 แห่งที่ import จาก Choowap มาโดยไม่ตรวจ กว่าจะแก้ครบต้องยิง Google Geocoding+Places API
  ตรวจทีละที่ย้อนหลัง **ระบบใหม่ควรตรวจสอบพิกัดตั้งแต่ตอน import ครั้งแรก** ถ้าจะดึงที่พักเพิ่มจาก
  แหล่งข้อมูลอื่นในอนาคต
- **ตั้งชื่อ team_code ให้เป็นมาตรฐานเดียวกัน** — ไฟล์จริงจากผู้ใช้สะกดไม่ตรงกันเสมอ (เจอ "Area" ปน
  "SMA" ปน "Admin" ในคอลัมน์เดียว) ต้อง normalize ตอน import เสมอ อย่าคาดหวังว่าไฟล์จะสะอาด

---

## 12. ไฟล์แนบมากับ handoff นี้

- `branches.csv` — ทะเบียนสาขา 230 แห่ง (รหัส, ชื่อ, อำเภอ, จังหวัด, พิกัด, สถานะ)
- `hotels.csv` — ทะเบียนที่พัก 207 แห่ง (ชื่อ, ราคา, อำเภอ, จังหวัด, พิกัด)
- `employees.csv` — รายชื่อพนักงาน 114 คน (รหัส, ชื่อ, ทีม, ชื่อเล่น, เพศ, Area ดูแล, เบอร์โทร, พิกัดบ้าน)
- Mockup HTML ที่ใช้คุยออกแบบทั้งหมด (ส่งแยกไว้ก่อนหน้าในแชท) — ใช้เป็น **spec ของหน้าตา/พฤติกรรม
  ที่ยืนยันแล้ว** อ่านซอร์สโค้ด (`<script>` ในไฟล์) เพื่อดู logic การคำนวณจริงทุกสูตรได้ตรงๆ เพราะ
  เขียนด้วย vanilla JS อ่านตรงไปตรงมา ไม่ผ่าน build step ใดๆ

**ถ้าต้องการเอกสารกระบวนการฝั่งมนุษย์** (ขั้นตอนที่ AREA ต้องทำจริงบน Choowap, เงื่อนไขจองล่วงหน้า
3 วัน, กฎพนักงานใหม่จองแยกใบ) ดูสไลด์ `SOP-มอบหมายจองที่พักให้ AREA.pptx` ที่ส่งไปก่อนหน้านี้ในแชท
— เป็นคนละเอกสารจากระบบนี้ (นั่นคือ SOP ของมนุษย์ ไม่ใช่ฟีเจอร์ในโค้ด)
